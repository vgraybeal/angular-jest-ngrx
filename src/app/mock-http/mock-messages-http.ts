import { HttpTestingController } from '@angular/common/http/testing';

export function mockMessagesHttp(httpMock: HttpTestingController, options: {isSuccess: boolean}) {} // here we would mock any http requests
